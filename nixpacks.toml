[phases.setup]
nixPkgs = ["python3", "nodejs", "chromium", "glib", "nss", "atk", "cups", "libXcomposite", "libXdamage", "libXrandr", "alsa-lib", "pango", "cairo", "mesa"]

[phases.install]
cmds = ["pip install -r requirements.txt", "python -m playwright install chromium --with-deps"]
