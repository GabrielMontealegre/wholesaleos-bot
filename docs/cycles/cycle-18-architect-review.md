# Next action: Claude architect review

Recommended reviewer: Claude Opus, High reasoning (use the current model available in
your Claude app). Codex implementation model: GPT-6 Astra, xhigh. No GPT-5.1 required.

```text
Review the Cycle 18 + Cycle 19 draft PR update on feat/cycle-18-evidence-packet-throughput-v1.
Base: 643b995. Read docs/cycles/cycle-18-evidence-packet-throughput.md and the checkpoint.
Codex is the implementer; you are the architect/reviewer. Gabriel is nontechnical.
Do not ask him to run commands, inspect diffs, or repeat technical release steps.

QA ONLY: no edits, merge, deploy, production access, batches, credentials or outreach.
Use the visualization checkout named in the checkpoint; leave other checkouts alone.

The frozen text-layer measurement overturned the identity-first prior: courthouse/trustee addresses
were becoming property identities. Check both candidate and card paths, complete-address
formatting without invented facts, and that OCR never gains normalized address authority.
Check independent contact/value/offer labels without changing state or comp gates.
Inspect real fixtures, aggregate counts, Cycle 19 OCR yield, all timed test results and
synthetic UI proof.

Result: one -> three distinct source-supported properties; three -> zero incorrect
normalized property addresses. No supported contact/comp gain. The notices have past
sale dates. Live inventory is UNREACHABLE. The Playwright browser resolver now tries the
default browser first and then discovers installed Playwright browser cache executables.
The pre-change launch proof reproduced default missing chromium_headless_shell-1234 and
explicit installed Chromium 1117 success.

Acceptance is now 16/16 for the bounded evidence package: cache-only OCR over Navarro
and Rockwall produced property-row traces across Hunt, Navarro and Rockwall. Verify the
critical safety condition: OCR rows remain review-only, with empty normalized_address,
no precise maps_url, and candidate/card paths agreeing. Do not turn this into source
expansion, live verification, contact hunting, comp changes, or OCR tuning.

Return READY/BLOCKED with reproduced findings. Give exactly one next Codex prompt,
using an available model such as GPT-6 Astra or GPT-5.4 Mini. Distinguish architecture
recommendation from release authorization: the present permission stops at draft PR.
Do not claim the SaaS is finished, the live dashboard changed, or callable leads exist
without measured evidence. Explain the next bottleneck in plain English.
```
